# src/

Publishable synthesis packages live at the **repo root**:

- [`../rgb_qwen/`](../rgb_qwen/) — Qwen Image Edit via ComfyUI
- [`../pcn_lidar/`](../pcn_lidar/) — PCN-LiDAR (WeatherGen → constant-\(N\))

This `src/` directory is reserved for a future installable Python package
(e.g. `fm_adverse_weather`) if we wrap those modules later.
